import {
  menuClickHandler,
  parallaxMoveHandler,
  parallaxCardHandler,
  // addCardClickHandler,
} from './js';

window.onload = () => {
  menuClickHandler();
  parallaxMoveHandler();
  parallaxCardHandler();
  // addCardClickHandler();
};